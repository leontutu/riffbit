import { describe, expect, it } from "vitest";

// Sanity check to verify test environment is configured correctly
describe("setup", () => {
    it("runs test successfully", () => {
        expect(true).toBe(true);
    });
});
